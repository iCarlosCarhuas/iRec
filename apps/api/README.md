# iRec API

NestJS 12.

## Desarrollo

Desde la raiz:

```bash
pnpm dev:api
```

## Contrato

- `/openapi.json`
- `/reference`
- schemas Zod compartidos en `packages/contracts`

En `0.1.0` solo se implementan endpoints de health. El resto se incorpora siguiendo `docs/NONTECH/ROADMAP.md`.
