# ADR-0006 — ThemeManifest

**Estado:** Accepted

La IA no produce código ejecutable libre.

Genera un manifiesto JSON validado mediante Zod, que selecciona componentes, layout, tokens y orden permitidos por iRec.
